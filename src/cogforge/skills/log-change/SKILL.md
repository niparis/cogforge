After any persisted change, run:

```
cogforge wiki log --message "<reason for change>" --session <session-path>
```

The CLI handles timestamping and file placement automatically.

Log at minimum:
- reason for the change
- related session file
