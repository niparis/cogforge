# Cogforge Knowledge Base

This repository contains a Cogforge-managed knowledge base.

The source code for the `cogforge` CLI lives elsewhere. Use the installed `cogforge` command to synchronize sources, process inbox items, validate structure, and write deterministic bookkeeping.

Start with:

```bash
cogforge status
cogforge config validate
```
